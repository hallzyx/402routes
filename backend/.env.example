NODE_ENV=development
PORT=8787

# Cronos Network (cronos-testnet or cronos-mainnet)
NETWORK=cronos-testnet

# Merchant address that receives payments (REPLACE WITH YOUR WALLET ADDRESS)
MERCHANT_ADDRESS=0x...

# Default price for API calls (in base units, e.g. 1000000 = $1 USDC with 6 decimals)
DEFAULT_PRICE=1000000

# USDC Token Address on Cronos Testnet
ASSET_ADDRESS=0x...

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
