[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "crypto-com-developer-platform-client>=1.1.1",
    "fastapi[standard]>=0.128.0",
]
